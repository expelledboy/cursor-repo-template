# /probe

Purpose: Run an explicit experiment (theory -> probe -> log).

Steps:
1) Load:
   - `docs/reference/agentos/operating-model-whitepaper.md`
   - `docs/reference/agentos/theory-registry.md`
   - `docs/reference/agentos/interplay-probes.md`
2) Select a theory ID (T*) and probe (P*).
3) Execute the probe procedure.
4) Record a short header log: signals used + probe + outcome (supports Validated/Falsified/Draft).
