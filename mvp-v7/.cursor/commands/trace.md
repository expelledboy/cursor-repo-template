# /trace

Purpose: Display the latest decision trace for the current focus (graphs evaluated, path taken, anchors used).

Steps:
1) Print the most recent `agentos.decision-trace/v1` for the active frame.
2) If no trace exists, print which graphs would be evaluated next and why.

